{
  "name": "dotnet-vuejs",
  "version": "1.0.0",
  "description": "A vue.js implementation using a dotnet webapi backend",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently -n \"api,web\" -c \"blue,green\" \"dotnet run --project api\" \"npm run dev --prefix web\"",
    "build": "concurrently -n \"api,web\" -c \"blue,green\" \"dotnet build api\" \"npm run build --prefix web\"",
    "type-check": "npm run type-check --prefix web"
  },
  "repository": {
    "type": "git",
    "url": "git@github-personal:aaroncroberts/dotnet-vuejs.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^10.0.3"
  }
}
